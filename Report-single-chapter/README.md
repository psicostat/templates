# Report Single Chapter

A single chapter report with Psicostat theme using R markdown and bookdown

## Requirements

The following R-packages are required

``` r
install.packages("rmarkdown")
install.packages("bookdown")
```
